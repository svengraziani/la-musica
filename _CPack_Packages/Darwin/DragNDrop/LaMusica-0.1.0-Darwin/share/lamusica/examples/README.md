# Example Projects

Example projects must use redistributable or generated assets only.

Current examples:

- `fixtures/empty.Project.lamusica`
- `fixtures/examples/generated-tone.Project.lamusica`

`generated-tone.Project.lamusica` uses synthesized clip data only and has no external media
dependencies.
